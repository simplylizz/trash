Install

To install package run this command:
> python setup.py install


Run

If package was installed you could run it with the folowing command:
> run_client.py

To run from sources run this:
> python run_client.py


Test Server

You could use test server which sends 20 random commands on connection
and then closes connection. Use command below to run server:
> python run_server.py
